# Supabase — use the SERVICE ROLE key (never expose to client)
SUPABASE_URL=https://ddsgkatwcwhxerfrgfyv.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Admin auth — hardcoded for MVP (replace with real auth before going public)
ADMIN_PASSWORD=changeme
# A random string used to sign session cookies — generate with: openssl rand -hex 32
ADMIN_SESSION_SECRET=replace_with_32_char_random_string
